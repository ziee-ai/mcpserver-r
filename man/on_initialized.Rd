% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{on_initialized}
\alias{on_initialized}
\title{Register a hook to run after the \code{initialize} handshake completes}
\usage{
on_initialized(mcp, fn)
}
\arguments{
\item{mcp}{An \code{McpServer}.}

\item{fn}{A function \verb{function(mcp, session)}.}
}
\value{
\code{mcp}, invisibly.
}
\description{
Useful for conditionally registering tools that depend on client
capabilities (e.g. only expose sampling-using tools when the client
declared \code{sampling}).
}
\examples{
srv <- new_server("demo")
on_initialized(srv, function(mcp, session) {
  # e.g. add_capability(mcp, ...) if session$client_capabilities$sampling
})
}
