% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{new_resource}
\alias{new_resource}
\title{Define a static MCP resource}
\usage{
new_resource(
  name,
  description,
  uri,
  mime_type = NULL,
  annotations = NULL,
  handler
)
}
\arguments{
\item{name}{Short identifier (must be unique).}

\item{description}{Human-readable description.}

\item{uri}{Resource URI.}

\item{mime_type}{Optional MIME type.}

\item{annotations}{Optional MCP annotations list.}

\item{handler}{A function \verb{function(params, ctx)}.}
}
\value{
A resource descriptor (tagged list).
}
\description{
Use this for resources with a fixed URI. For URIs that include a
parameter (e.g. \verb{demo://x/\{id\}}), use \code{\link[=new_resource_template]{new_resource_template()}} instead.
}
\details{
Handlers receive \code{params} (a named list including \code{uri}) and \code{ctx} and
should return a list with either a \code{text} or \code{blob} field plus an
optional \code{mimeType}. A character return value is wrapped automatically
into \code{list(text = value)}.
}
\examples{
new_resource("greeting", "A greeting", "demo://greeting",
             mime_type = "text/plain",
             handler = function(params, ctx) "hello")
}
