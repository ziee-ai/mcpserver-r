% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{new_server}
\alias{new_server}
\title{Create an MCP server}
\usage{
new_server(
  name,
  title = NULL,
  version = "0.1.0",
  instructions = NULL,
  capabilities = NULL,
  tools = list(),
  resources = list(),
  resource_templates = list(),
  prompts = list(),
  description = NULL,
  website_url = NULL,
  icons = NULL,
  strict_capabilities = FALSE,
  schema_validator = NULL
)
}
\arguments{
\item{name}{Short server identifier.}

\item{title}{Optional human-friendly title (defaults to \code{name}).}

\item{version}{Server version string.}

\item{instructions}{Optional text returned to the client during the
\code{initialize} handshake.}

\item{capabilities}{Optional named list of extra capability declarations
(e.g. \code{list(experimental = list(tasks = TRUE))}).}

\item{tools, resources, resource_templates, prompts}{Optional lists of
capabilities to register up front; equivalent to calling
\code{\link[=add_capability]{add_capability()}} on each.}

\item{description}{Optional one-line description shown in the
\code{serverInfo.description} field of the \code{initialize} response
(2025-11-25 BaseMetadata).}

\item{website_url}{Optional URL of a documentation / homepage,
surfaced as \code{serverInfo.websiteUrl}.}

\item{icons}{Optional list of icon descriptors (each a list with
\code{src} and \code{mimeType}), surfaced as \code{serverInfo.icons}.}

\item{strict_capabilities}{When \code{TRUE}, \code{\link[=set_request_handler]{set_request_handler()}}
refuses to register a handler for a method whose capability root
(\code{tools}, \code{resources}, \code{prompts}, ...) hasn't been declared on
this server.}

\item{schema_validator}{Optional pluggable validator returned by
\code{\link[=new_validator]{new_validator()}} to override the default jsonvalidate engine.}
}
\value{
An \code{McpServer} object.
}
\description{
Bundles a name, version, and capability declarations together with the
registries of tools, resources, resource templates, and prompts. The
returned object is mutable: register additional capabilities with
\code{\link[=add_capability]{add_capability()}} and start it with \code{\link[=serve_io]{serve_io()}} or \code{\link[=serve_http]{serve_http()}}.
}
\examples{
srv <- new_server("demo", version = "0.1.0")
srv
}
