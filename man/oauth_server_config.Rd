% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth-server.R
\name{oauth_server_config}
\alias{oauth_server_config}
\title{Configure an OAuth 2.1 authorization server}
\usage{
oauth_server_config(
  issuer,
  audience,
  signing_key = NULL,
  kid = "as-key-1",
  ttl_access = 3600L,
  ttl_refresh = 86400L,
  ttl_code = 60L,
  scopes_supported = c("mcp:read", "mcp:write"),
  auto_consent = TRUE,
  subject = "anonymous",
  consent_html_fn = NULL,
  client_store = new_oauth_store(),
  code_store = new_oauth_store(),
  token_store = new_oauth_store()
)
}
\arguments{
\item{issuer}{The HTTPS URL this AS uses as its \code{iss} claim and
metadata document base URL.}

\item{audience}{The resource indicator (the MCP server's public
URL) put into the \code{aud} claim.}

\item{signing_key}{An \code{openssl::rsa_keygen()} result. Auto-
generated (2048-bit) if \code{NULL}.}

\item{kid}{Key id put into the JWK and JWT header.}

\item{ttl_access}{Access-token lifetime in seconds.}

\item{ttl_refresh}{Refresh-token lifetime in seconds.}

\item{ttl_code}{Authorization-code lifetime in seconds.}

\item{scopes_supported}{Character vector of scopes advertised in
the metadata document.}

\item{auto_consent}{When \code{TRUE} (default), \verb{/authorize} responds
with a 302 redirect carrying the authorization code; useful for
tests. When \code{FALSE}, returns an HTML consent form.}

\item{subject}{Fixed \code{sub} claim. Real deployments would derive
this from the authenticated user.}

\item{consent_html_fn}{Optional \verb{function(req, cfg)} returning the
consent page HTML. Only consulted when \code{auto_consent = FALSE}.}

\item{client_store, code_store, token_store}{Pluggable in-memory
stores; defaults are \code{new_oauth_store()}.}
}
\value{
A list of class \code{"mcp_oauth_server_config"} with helper
accessors. Pass to \code{\link[=serve_http]{serve_http()}} via the \code{oauth_as} argument.
}
\description{
Builds an in-memory authorization server suitable for development,
integration tests, and conformance fixtures. Exposes five
endpoints (metadata, /authorize, /token, /register, /jwks) and
mints RS256 JWT access + refresh tokens signed with a key
generated on first call (override via \code{signing_key}).
}
\details{
\strong{Not for production}: this server has auto-consent enabled by
default, stores all state in memory, and does not implement
rate-limiting or audit logging. Use a real IdP (Auth0, Keycloak,
Okta) in production.
}
