% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-store.R
\name{new_event_store}
\alias{new_event_store}
\title{Construct an in-memory MCP event store}
\usage{
new_event_store(max_per_stream = 1000L)
}
\arguments{
\item{max_per_stream}{Maximum events retained per stream (default
1000). When exceeded the oldest events are evicted.}
}
\value{
An environment with \verb{$append}, \verb{$replay}, \verb{$drop_stream},
\verb{$size} methods.
}
\description{
Returns an object exposing \verb{$append(stream_id, payload)} and
\verb{$replay(stream_id, last_event_id, max)}. Replace it with a custom
implementation when you need cross-process resumability.
}
\examples{
es <- new_event_store(max_per_stream = 50)
id <- es$append("s1", "hello")
es$replay("s1", id)
}
