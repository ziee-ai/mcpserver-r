% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-error.R
\name{mcp_error}
\alias{mcp_error}
\title{Build a typed MCP error condition}
\usage{
mcp_error(message, code = -32603L, data = NULL)
}
\arguments{
\item{message}{Error message.}

\item{code}{Integer JSON-RPC error code (defaults to \code{-32603}
internal error). Use the constants in \code{mcp_error_codes()} or any
application-defined code in \verb{-32000..-32099}.}

\item{data}{Optional list with extra error data (echoed in the
response's \code{error.data} field).}
}
\value{
A condition that, when caught by the dispatcher, is turned
into a JSON-RPC error envelope with the supplied code/data.
}
\description{
Handlers can \code{stop()} with the value returned here to surface a
specific JSON-RPC error code and optional structured \code{data} field at
the protocol layer. Mirrors the TypeScript SDK's \code{McpError} class.
}
\examples{
err <- mcp_error("invalid input",
                 code = mcp_error_codes()$invalid_params,
                 data = list(arg = "x"))
inherits(err, "mcp_error")
}
