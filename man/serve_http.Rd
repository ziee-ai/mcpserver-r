% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport_http.R
\name{serve_http}
\alias{serve_http}
\title{Run an MCP server over Streamable HTTP}
\usage{
serve_http(
  mcp,
  host = "127.0.0.1",
  port = 3000L,
  path = "/mcp",
  allowed_origins = c("http://localhost", "http://127.0.0.1"),
  allowed_hosts = NULL,
  require_origin = TRUE,
  tls = NULL,
  max_event_log = 1000L,
  auth = NULL,
  oauth_as = NULL,
  admin = NULL,
  daemons = 4L,
  stateless = FALSE,
  enable_json_response = TRUE,
  onsessioninitialized = NULL,
  onsessionclosed = NULL,
  block = TRUE
)
}
\arguments{
\item{mcp}{An \code{McpServer}.}

\item{host}{Address to bind to. Default \code{"127.0.0.1"}.}

\item{port}{TCP port.}

\item{path}{URL path for the MCP endpoint.}

\item{allowed_origins}{Character vector of allowed \code{Origin} prefixes.}

\item{allowed_hosts}{Optional character vector of allowed values for
the HTTP \code{Host} header (bare host or \code{host:port}). When \code{NULL} (the
default) the Host check is skipped.}

\item{require_origin}{Whether \code{Origin} is required (default \code{TRUE}).}

\item{tls}{Optional TLS configuration from \code{nanonext::tls_config()}.}

\item{max_event_log}{Maximum events retained per session for SSE replay.}

\item{auth}{Optional auth configuration from \code{\link[=oauth_config]{oauth_config()}}.}

\item{oauth_as}{Optional authorization-server configuration from
\code{\link[=oauth_server_config]{oauth_server_config()}}. When set, the server additionally
exposes \verb{/.well-known/oauth-authorization-server}, \verb{/authorize},
\verb{/token}, \verb{/register}, and \verb{/jwks}. If \code{auth} is also \code{NULL}
a matching resource-server config is derived automatically so a
single process can both issue and verify its own bearer tokens.}

\item{admin}{Optional admin REST + SPA configuration. Either \code{TRUE}
(read \code{MCPSERVER_ADMIN_TOKEN} from the environment, reuse
\code{oauth_as$store}), or a list with fields \code{bootstrap_token} (opaque
string), \code{store} (\code{\link[=new_mcp_store]{new_mcp_store()}} result; defaults to
\code{oauth_as$store}), \code{ui} (logical; serves the bundled React SPA at
\verb{/admin/ui/*} when \code{TRUE}), \code{max_ttl} (max access-token ttl in
seconds), and \code{ui_dist_dir} (override the bundled SPA path).
When set, the server mounts \verb{/admin/healthz}, \verb{/admin/users},
\verb{/admin/users/\{id\}}, \verb{/admin/users/\{id\}/tokens},
\verb{/admin/tokens/mint}, \verb{/admin/tokens/\{jti\}/revoke} and (when
\code{ui = TRUE}) the SPA static handler.}

\item{daemons}{Number of \code{mirai} daemons.}

\item{stateless}{When \code{TRUE}, the server does not allocate or
validate \code{Mcp-Session-Id} headers and every request is treated as
a fresh self-contained interaction (no SSE GET stream, no
resource subscriptions, no server-to-client requests). Mirrors
the TS SDK's \code{sessionIdGenerator = undefined} mode.}

\item{enable_json_response}{When \code{TRUE} (default), POST responses
are always returned as a single JSON envelope. Reserved for a
future SSE-on-POST mode; today only \code{TRUE} is supported.}

\item{onsessioninitialized}{Optional callback \verb{function(session_id)}
invoked when a new session is allocated.}

\item{onsessionclosed}{Optional callback \verb{function(session_id)}
invoked when a session is torn down (DELETE or eviction).}

\item{block}{Whether to block in the server's \verb{$serve()} event loop. If
\code{FALSE}, returns the running \code{nanoServer} object so the caller can
drive \code{later::run_now()} themselves; useful for tests.}
}
\value{
Invisibly, the running \code{nanoServer} object (only meaningful when
\code{block = FALSE}).
}
\description{
Implements the MCP 2025-06-18 Streamable HTTP transport on top of
\code{nanonext::http_server()}:
}
\details{
\itemize{
\item \verb{POST <path>} — clients send JSON-RPC requests/notifications. The
server replies with JSON for requests or \verb{202 Accepted} for
notifications/responses.
\item \verb{GET <path>} — clients open a long-lived SSE stream that the server
uses for asynchronous notifications and outgoing requests
(sampling, elicitation, roots).
\item \verb{DELETE <path>} — clients terminate the session.
}

Session lifecycle is keyed by the \code{Mcp-Session-Id} HTTP header. The
\code{Origin} header is validated against \code{allowed_origins} to defend
against DNS rebinding attacks; missing \code{Origin} is rejected unless
\code{require_origin = FALSE}. The \code{MCP-Protocol-Version} header is
enforced on all non-\code{initialize} requests.

\code{Last-Event-ID} on \code{GET} triggers replay of events appended after that
id from a per-session ring buffer capped by \code{max_event_log}.

Requests are served concurrently: tool/resource/prompt handlers run in
\code{mirai} daemons and the \code{POST} reply is streamed back when the handler
resolves, so a slow call from one client does not block other clients.
(Tools that themselves call back to the client — sampling, elicitation,
roots — still run on the transport thread.)
}
