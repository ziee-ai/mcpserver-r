% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{add_capability}
\alias{add_capability}
\title{Register a tool, resource, resource template, or prompt on a server}
\usage{
add_capability(mcp, capability)
}
\arguments{
\item{mcp}{An \code{McpServer} returned by \code{\link[=new_server]{new_server()}}.}

\item{capability}{A capability descriptor built with \code{\link[=new_tool]{new_tool()}},
\code{\link[=new_resource]{new_resource()}}, \code{\link[=new_resource_template]{new_resource_template()}}, or \code{\link[=new_prompt]{new_prompt()}}.}
}
\value{
\code{mcp}, invisibly.
}
\description{
Returns the server invisibly so calls can be chained.
}
\examples{
srv <- new_server("demo")
tool <- new_tool("echo", "Echo text",
                 input_schema = schema(list(text = property_string(required = TRUE))),
                 handler = function(args, ctx) response_text(args$text))
add_capability(srv, tool)
}
