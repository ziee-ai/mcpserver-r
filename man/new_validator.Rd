% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validator.R
\name{new_validator}
\alias{new_validator}
\title{Construct an MCP schema validator}
\usage{
new_validator(engine = c("ajv", "none"))
}
\arguments{
\item{engine}{\code{"ajv"} (default, via jsonvalidate) or \code{"none"}
(accepts every payload — useful for tests and stubs).}
}
\value{
A function \verb{function(schema) -> function(args) -> list(ok, errors)}.
}
\description{
Returns a callable: given a JSON Schema list, returns a function
that validates a payload and produces \code{list(ok, errors)}.
}
\examples{
v <- new_validator()
check <- v(schema(list(x = property_integer(required = TRUE))))
check(list(x = 42L))
}
