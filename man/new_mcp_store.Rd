% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-store.R
\name{new_mcp_store}
\alias{new_mcp_store}
\title{Open a users + tokens store}
\usage{
new_mcp_store(driver = c("memory", "sqlite"), path = NULL)
}
\arguments{
\item{driver}{Either \code{"memory"} (default; ephemeral env-based) or
\code{"sqlite"} (persistent via DBI/RSQLite).}

\item{path}{For \code{driver = "sqlite"}, filesystem path to the database
file. Created if absent.}
}
\value{
A list with class \code{"mcp_store"} and elements \verb{$users},
\verb{$tokens}, \verb{$close()}, \verb{$revocation_store()}.
}
\description{
Returns an object with \verb{$users} (CRUD on user records) and \verb{$tokens}
(CRUD on issued-JWT metadata) namespaces. Both share one backing
connection. Pass the returned object to \code{\link[=oauth_server_config]{oauth_server_config()}} as
the \code{store} argument.
}
