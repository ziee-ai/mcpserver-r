% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth-server.R
\name{oauth_mint_user_token}
\alias{oauth_mint_user_token}
\title{Mint a user-bound access token}
\usage{
oauth_mint_user_token(cfg, user_id, scopes, ttl = NULL, name = "token")
}
\arguments{
\item{cfg}{An \code{\link[=oauth_server_config]{oauth_server_config()}} result with a \code{store} set.}

\item{user_id}{Id of an existing user in \code{cfg$store$users}.}

\item{scopes}{Character vector of scopes to embed in the token.}

\item{ttl}{Lifetime in seconds (defaults to \code{cfg$ttl_access}).}

\item{name}{Human label stored alongside the token row (e.g.
\code{"ci-runner"}). Must be unique per user.}
}
\value{
A list with \code{jti}, \code{token} (the JWT string), and
\code{expires_at} (ISO 8601, UTC). The JWT is returned \strong{once}; only
metadata is persisted server-side.
}
\description{
Signs an RS256 JWT with \code{sub = user_id} and a fresh \code{jti}, and
persists the metadata row in \code{cfg$store$tokens} for instant
revocation. Requires \code{cfg} to have been built with a \code{store} arg.
}
