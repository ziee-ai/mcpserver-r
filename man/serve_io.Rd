% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport_stdio.R
\name{serve_io}
\alias{serve_io}
\title{Run an MCP server over stdio}
\usage{
serve_io(mcp, log_path = NULL, daemons = 4L)
}
\arguments{
\item{mcp}{An \code{McpServer} returned by \code{\link[=new_server]{new_server()}}.}

\item{log_path}{Optional path to redirect \code{stderr} to. When \code{NULL},
warnings and messages flow to the inherited stderr. The stdout stream
is reserved for the MCP protocol; never write to it directly.}

\item{daemons}{Number of \code{mirai} daemons to spawn.}
}
\value{
\code{NULL}, invisibly. The function blocks until \code{stdin} closes.
}
\description{
Reads newline-delimited JSON-RPC messages from \code{stdin}, dispatches them
to the server's handlers, and writes responses to \code{stdout}. Tool
handlers run inside \code{mirai} daemons so long-running calls do not block
the main read/dispatch loop.
}
