% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{oauth_config}
\alias{oauth_config}
\title{Configure OAuth 2.1 bearer-token validation}
\usage{
oauth_config(
  issuer,
  audience,
  jwks_uri,
  required_scopes = character(0L),
  introspection_url = NULL,
  introspection_basic = NULL,
  leeway = 30L
)
}
\arguments{
\item{issuer}{Expected \code{iss} claim and base URL for the resource
metadata pointer in \code{WWW-Authenticate}.}

\item{audience}{Required \code{aud} claim (this server's resource indicator).}

\item{jwks_uri}{JWKS endpoint URL.}

\item{required_scopes}{Optional character vector of scopes that must all
be present.}

\item{introspection_url}{Optional RFC 7662 introspection endpoint.}

\item{introspection_basic}{Optional named list with \code{client_id} and
\code{client_secret} used for HTTP Basic auth on the introspection call.}

\item{leeway}{Clock-skew tolerance in seconds (default 30).}
}
\value{
A list with class \code{"mcp_oauth_config"}.
}
\description{
Returns a configuration object accepted by \code{\link[=serve_http]{serve_http()}}'s \code{auth}
argument. Two flows are supported:
}
\details{
\itemize{
\item \strong{JWT} (default): tokens are decoded and validated locally against a
JWKS endpoint. \code{iss}, \code{aud}, \code{exp}, \code{nbf}, and required scopes are
checked, with \code{leeway} seconds of clock skew tolerated.
\item \strong{Introspection}: when \code{introspection_url} is supplied, opaque
tokens are validated by POSTing to that endpoint per RFC 7662.
}
}
