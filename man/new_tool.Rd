% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{new_tool}
\alias{new_tool}
\title{Define an MCP tool}
\usage{
new_tool(
  name,
  description,
  input_schema,
  output_schema = NULL,
  annotations = NULL,
  handler,
  tasks = FALSE,
  bidirectional = FALSE
)
}
\arguments{
\item{name}{Tool identifier (must be unique within a server).}

\item{description}{Human-readable description.}

\item{input_schema}{A schema built with \code{\link[=schema]{schema()}}.}

\item{output_schema}{Optional schema for the tool's structured content.}

\item{annotations}{Optional named list of MCP annotations (e.g.
\code{list(readOnlyHint = TRUE)}).}

\item{handler}{A function \verb{function(args, ctx)}.}

\item{tasks}{Whether the tool participates in the experimental
tasks lifecycle (default \code{FALSE}).}

\item{bidirectional}{Set \code{TRUE} for tools that issue server-to-client
requests (\code{ctx$request_sampling()}, \code{ctx$request_elicitation()},
\code{ctx$request_roots()}). Such handlers execute on the transport
thread instead of inside a \code{mirai} daemon so they can access the
live pending-request table. The transport stays responsive because
the handler yields via \code{later::run_now()} while waiting on the
client.}
}
\value{
A tool descriptor (tagged list) for use with \code{\link[=add_capability]{add_capability()}}.
}
\description{
A tool packages a name, JSON-Schema declaration of its arguments, an
optional output schema, optional MCP annotations, and an R handler
function that the dispatcher calls with parsed arguments. The handler
should return a value from one of the \verb{response_*()} constructors.
}
\details{
Handlers receive two arguments: \code{args} (a named list of the validated
input arguments) and \code{ctx} (a context object exposing progress,
logging, sampling, elicitation, and roots helpers).
}
\examples{
new_tool(
  name = "echo",
  description = "Echo back the input",
  input_schema = schema(list(text = property_string(required = TRUE))),
  handler = function(args, ctx) response_text(args$text)
)
}
